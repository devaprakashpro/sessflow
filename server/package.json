{
  "name": "sessflow-mesh-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Sessflow Mesh Sync server — self-hosted, runs on your Tailscale node. Sync + page archive + AI ask-your-tabs.",
  "bin": { "sessflow-mesh": "src/index.js" },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.1",
    "better-sqlite3": "^11.3.0",
    "cheerio": "^1.0.0",
    "hono": "^4.6.3",
    "ws": "^8.18.0"
  }
}
